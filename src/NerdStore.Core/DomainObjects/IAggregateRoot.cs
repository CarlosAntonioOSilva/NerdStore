namespace NerdStore.Core.DomainObjects
{
    /*
        Interfaces de marcação

        - muitas ferramentas as utilizam
        - a princípio, não é implementado nada nessa interface (está vazia)
        - é utilizar para indicar se a classe que a instancia é uma raiz de agregação
        - permite distinguir qual classe é uma entidade e qual classe é uma raiz de agregação
    */
    public interface IAggregateRoot
    {
    }
}
